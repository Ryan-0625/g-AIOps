target-version = "py312"

select = ["E", "F", "I", "N", "W"]

ignore = [
    "E501",  # line length — handled by formatter
    "N802",  # function name lowercase (allow asyncio handlers)
]

line-length = 100

[per-file-ignores]
"__init__.py" = ["F401"]
