[project]
name = "gaiops-brain"
version = "0.1.0"
description = "gAIOps Brain - Decision Engine"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.1.0",
    "aiohttp>=3.9.0",
    "pydantic>=2.5.0",
    "pyyaml>=6.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["."]
