# Brain dev image — Python with mounted source for quick iteration
FROM python:3.12-alpine

RUN apk add --no-cache tzdata ca-certificates

WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 9091

CMD ["python", "main.py"]
